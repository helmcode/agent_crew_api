# AgentCrew API - Environment Variables
# Copy this file to .env and fill in the values

# Anthropic API key for Claude Code agents
ANTHROPIC_API_KEY=your-api-key-here

# NATS authentication token (shared between API server and NATS)
NATS_AUTH_TOKEN=your-nats-token-here

# NATS server URL (used by the sidecar process)
NATS_URL=nats://nats:4222

# SQLite database file path
DATABASE_PATH=agentcrew.db

# HTTP server listen address
LISTEN_ADDR=:8080

# Container runtime: "docker" (default) or "kubernetes"
RUNTIME=docker

# Kubeconfig path (only required when RUNTIME=kubernetes and running outside a cluster)
# KUBECONFIG=/path/to/kubeconfig
